version: '3'

services:
  qdrant:
    image: qdrant/qdrant
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - ./qdrant_data:/qdrant/storage

  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - QDRANT_HOST=qdrant
    depends_on:
      - qdrant